# NIPUN Compass — Streamlit app runtime dependencies
# Pinned exact versions (tested 2026-04-17, Python 3.11+)
# Extraction pipeline deps (pdfplumber, camelot-py) are NOT listed here —
# they are only needed for the one-time data build step and require system
# packages (poppler, ghostscript) that are unavailable on Streamlit Cloud.

streamlit==1.56.0
pandas==3.0.2
numpy==2.4.4
plotly==6.7.0
scikit-learn==1.8.0
sentence-transformers==5.4.1
chromadb==1.5.8
reportlab==4.4.10
python-dotenv==1.2.2
openpyxl==3.1.5
